name: Build MobileSlideShowHook

on:
  workflow_dispatch:
    inputs:
      build_scheme:
        description: "选择编译类型"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - rootless
          - roothide

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ inputs.build_scheme }}
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Theos
        uses: waruhachi/theos-action@v2.4.10
        with:
          theos-src: "roothide/theos"
          theos-branch: "master"
          sdks-src: "theos/sdks"
          sdks-branch: "master"
          libgcuniversal: "false"
          altlist: "false"
          comet: "false"
          orion: "false"

      - name: Prepare output
        run: |
          rm -rf artifacts
          mkdir -p artifacts/rootless artifacts/roothide

      - name: Build rootless
        if: ${{ inputs.build_scheme == 'all' || inputs.build_scheme == 'rootless' }}
        run: |
          rm -rf packages
          mkdir -p packages
          sed -i '' 's/^Architecture:.*/Architecture: iphoneos-arm64/' control
          make clean
          make package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless
          ROOTLESS_DEB="$(find packages -maxdepth 1 -type f -name '*.deb' | head -n 1)"
          test -n "$ROOTLESS_DEB"
          cp -v "$ROOTLESS_DEB" artifacts/rootless/

      - name: Build roothide
        if: ${{ inputs.build_scheme == 'all' || inputs.build_scheme == 'roothide' }}
        run: |
          rm -rf packages
          mkdir -p packages
          sed -i '' 's/^Architecture:.*/Architecture: iphoneos-arm64e/' control
          make clean
          make package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=roothide
          ROOTHIDE_DEB="$(find packages -maxdepth 1 -type f -name '*.deb' | head -n 1)"
          test -n "$ROOTHIDE_DEB"
          cp -v "$ROOTHIDE_DEB" artifacts/roothide/

      - name: Verify rootless artifact
        if: ${{ inputs.build_scheme == 'all' || inputs.build_scheme == 'rootless' }}
        run: |
          test "$(find artifacts/rootless -maxdepth 1 -type f -name '*.deb' | wc -l | tr -d ' ')" = "1"

      - name: Verify roothide artifact
        if: ${{ inputs.build_scheme == 'all' || inputs.build_scheme == 'roothide' }}
        run: |
          test "$(find artifacts/roothide -maxdepth 1 -type f -name '*.deb' | wc -l | tr -d ' ')" = "1"

      - name: Upload rootless
        if: ${{ inputs.build_scheme == 'all' || inputs.build_scheme == 'rootless' }}
        uses: actions/upload-artifact@v4
        with:
          name: MobileSlideShowHook-rootless
          path: artifacts/rootless/*.deb
          if-no-files-found: error

      - name: Upload roothide
        if: ${{ inputs.build_scheme == 'all' || inputs.build_scheme == 'roothide' }}
        uses: actions/upload-artifact@v4
        with:
          name: MobileSlideShowHook-roothide
          path: artifacts/roothide/*.deb
          if-no-files-found: error
